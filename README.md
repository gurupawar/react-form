# Installationa and Instruction to Start Sever

## Use below cmd in terminal to start json server

`npx json-server --watch serverdb.json --port 5500`

## start react app use following commands

`npm install`

`npm start`
